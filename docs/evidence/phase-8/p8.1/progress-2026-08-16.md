# P8.1 Progress — 2026-08-16

Human UAT remains deferred by explicit user instruction. No real provider traffic was enabled.

## Live staging already verified

- Railway project `education-erp-backend` (`6d24ea5d-99d4-4614-b608-569be198e321`), environment `production`.
- `web` is SUCCESS at `https://web-production-7580e.up.railway.app`.
- `GET /api/method/university_erp.api.health.live` and `GET /api/method/university_erp.api.health.ready` return HTTP 200.
- MariaDB 11.8 and Redis are running with persistent volumes.
- One-shot `bootstrap` previously created the site and is no longer needed for `bench new-site`.

## Work done in this slice

- Added Cloudflare DNS/WAF/R2 templates under `deploy/cloudflare/`. No Cloudflare zone, DNS record, or R2 bucket was created.
- Added staging probe and alert templates under `deploy/monitoring/`.
- Added `scripts/operations/backup-mariadb.sh` and `scripts/operations/restore-mariadb.sh` plus `docker/backup.Dockerfile` and `deploy/railway/backup.railway.toml`.
- Added a constrained `combined` runtime role and `deploy/railway/combined.railway.toml` so one replica can run `bench schedule` plus `bench worker --queue short,default,long`.
- Added `.railwayignore` so Railway uploads do not walk upstream apps or `node_modules`.
- Removed a leftover container reparse point at `apps/university_erp/university_erp/public/node_modules` that blocked `railway up`.
- `scripts/ci/verify-deployment.ps1` passed after the new artifacts were added.

## Railway free-plan constraint

Creating `scheduler`, `worker-short`, `worker-long`, `websocket`, `migrate`, or `backup` failed with:

```text
Free plan resource provision limit exceeded
```

The completed `bootstrap` service was therefore retargeted to the combined scheduler/worker start command. An initial Railpack redeploy failed because it ignored the Dockerfile. A Dockerfile `railway up` succeeded as deployment `71c6041d-f258-4317-8a16-6df0f94b4840`. Runtime logs show scheduled jobs completing on the `long` queue for site `web-production-7580e.up.railway.app`.

## Restore proof status

- Restore automation exists and refuses to overwrite the live database unless `ALLOW_LIVE_RESTORE=1`.
- A live isolated restore on Railway MariaDB did not complete in this session: `railway ssh` hung after key registration (`education-erp-operator`).
- Off-host backup copies and PITR are still not provisioned.

## Still required before P8.1 can close

- Isolated restore proof with matching source/target table counts.
- Railway Hobby/Pro or equivalent if dedicated worker, WebSocket, migrate, and backup services are required.
- Published registry digest and SBOM verification.
- Approved Cloudflare/R2 credentials before any edge or object-storage cutover.
