[build]
dockerfilePath = "docker/Dockerfile"

[deploy]
startCommand = "/home/frappe/frappe-bench/start-service.sh web"
healthcheckPath = "/api/method/university_erp.api.health.ready"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
